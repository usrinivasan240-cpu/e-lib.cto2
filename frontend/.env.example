# Optional API base path.
# Leave unset for same-origin deployments where the API is available under /api.
# If you deploy the frontend under a sub-path, you can set this to something like: /my-subpath
VITE_API_URL=
